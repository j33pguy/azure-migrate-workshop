# Module 3 has moved

The Hyper-V workshop now uses [Module 3: Cutover and stateful validation](Module-3-Stateful-Migration.md).

The earlier instructions installed guest Mobility Service agents and a separate replication appliance for SQL and Node.js. That is a different source workflow and is not required for these Hyper-V VMs. This page remains as a redirect for previously shared links; follow the replacement exercise.
